import { describe, test } from "bun:test";

describe("FaceService Initialization", () => {
  test("should initialize with default models from GitHub", async () => {
    // Lorem
  }, 30000); // Increase timeout for download
});
